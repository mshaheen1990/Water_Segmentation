base_experiment_config: configs/experiments/unet_earlyfusion.yaml
output_dir: results/improvement_stage_2
thresholds: [0.5, 0.56, 0.6]
candidates:
  - model_variant: unet
    epochs: 24
    tiles_per_image: 2
  - model_variant: attention_unet
    epochs: 20
    tiles_per_image: 2
  - model_variant: se_unet
    epochs: 20
    tiles_per_image: 2
  - model_variant: tfn_unet
    epochs: 20
    tiles_per_image: 2
